import React from "react"

// Leer el Token de LocalStorage que lo guardamos anttreiormente "localStore.getItem("token")
// Debes hacer un fetch colocando la autorización en el header (token)


export const Perfil = () => {
    return (
        <div></div>
    );
};